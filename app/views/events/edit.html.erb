<section class="container">
  <div>
    <%= link_to t('.back'), root_path, class: "btn btn-gradient mt-3" %>
  </div>
  <div class="d-flex justify-content-center">
    <h2><%= t('.title') %></h2>
  </div>

  <%= render 'form', event: @event %>
</section>
